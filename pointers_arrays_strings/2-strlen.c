#include "main.h"
#include <string.h>
/**
 *_strlen - for print  the characteres
 *@s: it is a pointers
 *Return: return the value of quanty characteres
 */

int _strlen(char *s)
{
	int you_can;

	you_can = strlen(s);
		return (you_can);

}
