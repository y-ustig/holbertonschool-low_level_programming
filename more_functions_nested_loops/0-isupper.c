#include "main.h"
/**
 *_islower - print the 1 when is mayus or 0 when is lower
 *@c: la cantidad
 *Return: retorna los valores
 */
int _isupper(int c)
{
if (c <= 'Z' && c >= 'A')
{
return (1);
}
else
{
return (0);
}
}
