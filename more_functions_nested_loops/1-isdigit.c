#include "main.h"
/**
 *_isdigit - print the 1 when is mayus or 0 when is lower
 *@c: la cantidad
 *Return: retorna los valores
 */
int _isdigit(int c)
{
if (c <= 0 && c >= 9)
{
return (1);
}
else
{
return (0);
}
}
