#include <stdio.h>
#include "main.h"
/**
 *mul - functions for multiplicar more numbers
 *@b: the number z
 *@a: the number x
 *@resultado: producto
 *Return: retorna los valores
 */
int mul(int a, int b)
{
int resultado = a * b;
{
printf("%d", resultado);
}
return (0);
}
